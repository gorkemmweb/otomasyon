```yaml
name: Claude Testi

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Claude entegrasyonunu kontrol et
        run: |
          echo "GitHub Actions çalışıyor."
          echo "Claude entegrasyonu için sonraki adımı test ediyoruz."
```
